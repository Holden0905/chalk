import Stub from "@/components/Stub";
export const metadata = { title: "Bets · Chalk" };
export default function BetsPage() {
  return (
    <Stub title="Bets" phase="Phase 3">
      The bet log, with closing line value and a running profit. Runs on the
      command line today.
    </Stub>
  );
}
